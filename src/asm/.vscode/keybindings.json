// Place your key bindings in this file to override the defaults.
// Needs to go in %APPDATA%\Code\User\keybindings.json (on Windows)
[
    {
        "key": "shift+ctrl+f8", 
        "command": "workbench.action.tasks.runTask",
        "args": "make emu"
    },
    {
        "key": "shift+ctrl+f9", 
        "command": "workbench.action.tasks.runTask",
        "args": "make sync"
    }
]